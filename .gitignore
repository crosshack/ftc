.DS_Store
Tmp/
_to_delete/
_setup/

# クラウド同期の競合コピー等で誤って生成される重複ファイル(例: "center 2.html")を無視
* [0-9].*
* [0-9][0-9].*

# Coworkのチャット添付が自動保存されるフォルダ(2026-09-12追加)。
# デスクトップアプリが接続フォルダ配下に勝手に作るもので、サイトの配信物ではない。
# 追跡したままだとNetlifyで本番配信されてしまうため、除外する。
Claude outputs/
