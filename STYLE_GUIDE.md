# FOOTBALLTACTICS.CLUB スタイルガイド

このサイトのページを新規作成・編集する前に、必ずこのファイルを読むこと。
これまでのやり取りで決まったルールをまとめたもの。ルールが増えた場合はこのファイルに追記する。

## 表記ルール

- カッコは全角（）を使う。半角()は使わない。
  - 例外: SVG/CSSの `url(#id)` のようなコード内の半角カッコは対象外。
- サイト名は「FOOTBALLTACTICS.CLUB」（スペースなし）。「FOOTBALL TACTICS CLUB」は使わない。
- 「FOOTBALLTACTICS.CLUB とは」の表記でaboutページを指す。

## サイト名・ロゴの装飾

- `.brand-tac` … 色のみ（`color: var(--accent)`、緑）。「TACTICS」部分に使う。
- `.site-title` … フォントのみ（`font-family: var(--font-head)`、Oswald）。
- 両方必要な場合: `<span class="site-title">FOOTBALL<span class="brand-tac">TACTICS</span>.CLUB</span>`
- パンくずリストの「FOOTBALLTACTICS.CLUB」表記は `.site-title`（フォントのみ）を使い、`.brand-tac`（色）は付けない。

## 基礎知識・戦術まとめのカテゴリ/命名

- カテゴリ名: オフェンス / ディフェンス / スペシャルチーム（「〜の基礎」「〜戦術」は付けない）
- 基礎知識の各コンテンツ名は「〜の基礎」で統一（例: オフェンスポジションの基礎）
- 戦術まとめの各コンテンツ名は「〜まとめ」で統一（例: オフェンスフォーメーションまとめ）
- 例外: 「カバレッジの基礎」（defense-coverage-basics.html）は改名対象外。カテゴリタグのみ変更する。

## ページ構成の共通パターン（basics/tactics 詳細ページ）

1. `<title>` タグ
2. breadcrumb（ページ上部）
3. `.article-head` または `.page-head` 内の `.tag.tag-flag`（カテゴリ、オレンジ表示）
4. `<h1>` タイトル。サブタイトルがある場合は `<br>` で改行してから `<span class="subtitle">` を続ける。
5. `.meta`（読了目安など）
6. `.article-body`（本文）
7. `.reference-links`（出典。該当する場合のみ。本文末尾・`</article>` の直前に配置）
8. `.page-nav`（前後ページへのリンク）
9. breadcrumb（ページ下部、footer直前）
10. footer

## 出典（参考文献）の掲載基準

以下のいずれかに該当する場合は出典を明記する。

- 特定の外部情報源（記事・書籍・発言・データセットなど）に依拠している場合。独自性の強弱は問わない。
- 数値・統計データを主張の根拠にしている場合。一般に公開されている数字でも、正確性を担保するため出典を明記する。

出典が不要なもの:

- ルールブックレベルの一般知識、複数の情報源で広く共有されている定説。
- 試合のスコアなど、誰が見ても同じ一つの答えがある公知の事実。

出典を追加する際は、必ず検索で数値の正確性を検証してから記載する（サイト内で生成した数値が実データとズレているケースがあったため）。

## 出典ボックス（`.reference-links`）のデザイン仕様

- 構造は `.see-also`（もっと詳しく）と同じ箱組みだが、アクセントカラーはオレンジ（`var(--flag)`）。
- ラベルは「出典」（緑の `.see-also` と区別するため）。
- リンクは通常ウェイト（太字にしない）、色は `var(--text-dim)`（明度を落とした白）。
- 複数項目は `<ul><li><a>` のリスト形式。
- 余白: ラベル→最初の項目 12px、項目間 8px、1項目が折り返した場合の行間 4px。
  - `<li>` と `<a>` の両方に `font-size` と `line-height` を明示すること。`<li>` に明示しないと、body全体の `line-height: 1.8` が「隠れたstrut」として効いてしまい、意図した余白にならない（実際に発生したバグ）。
- CSSクラス定義は `assets/css/style.css` の `.reference-links` ブロックを参照。

## 「まとめ」セクションの範囲

- ページ末尾の「まとめ」は、そのページの内容を要約することに徹する。次ページへの誘導・言及（「次のページでは〜」等）は書かない。次コンテンツへの導線は画面下部の `.page-nav`（前後ページへ）が担うため、本文と役割が重複しないようにする。

## CSSのキャッシュ対策

- 全ページの `<link rel="stylesheet" href=".../style.css">` および `tactics.css` には、末尾にバージョンクエリ（例: `?v=1`）を付与している。
- `style.css` または `tactics.css` を編集した際は、このバージョン番号を1つ繰り上げ、全ページの該当 `<link>` タグに反映すること（サイト全体に対して一括置換でよい）。これを怠ると、ブラウザ側のキャッシュにより変更が反映されないことがある。

## 実装時に気をつけること（過去に発生した不具合パターン）

- **第二のスタイルシート**: `tactics/*.html` は `style.css` に加えて `assets/css/tactics.css` を読み込む。tactics詳細ページでCSSの変更が反映されない場合、このファイルに同名セレクタで上書きされていないか確認する。
- **`.page-head` と `.article-head` の並行存在**: 見た目は同じでもクラス名が違う。「全詳細ページに適用」と言われたら両方に当てる。
- **JSでレンダリングされる要素**: フィルタピルなど、見た目上の親要素とCSSで実際にスタイルを当てるべきDOM要素が違うことがある。`<script>` 内の `document.getElementById(...)` を確認してから対応する。
- **body の `line-height: 1.8` の継承**: 明示的に line-height を指定していない要素（特に小さいラベルやリスト項目）に効いてきて、意図しない余白になることがある。余白の見た目がおかしいときはComputedスタイルで確認する。

## 作業前のチェックリスト

新しいページを作る/既存ページを大きく編集する前に:

1. このファイルを読む。
2. 命名・カテゴリ・カッコ表記のルールに沿っているか確認する。
3. 数値を書く場合は出典基準に照らして検索で検証する。
4. 完了後、タグバランス（開閉タグの数）とSVGのXML妥当性をチェックする。
